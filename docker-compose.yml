services:
  combat-record:
    build: .
    container_name: combat-record
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      DB_PATH: /app/data/combat-record.db
    volumes:
      - ./data:/app/data
